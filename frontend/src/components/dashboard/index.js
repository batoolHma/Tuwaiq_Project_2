import React from 'react';
import { Card,Row,Col } from 'react-bootstrap';
const Dashboard = () => {
	// const [data, setData] = useState([]);
	return <div className="App">{ 
	}
	<h1>this is dashboard here will be cards</h1>
</div>
};

export default Dashboard;


/* <div onClick = {()=>{
	navigate(`/${elem.pageName}`)
}}>
<img src={elem.img}>
<p>{elem.title}</p>
<p>{elem.int}</p>
<p>{elem.number}</p>
</div>
*/